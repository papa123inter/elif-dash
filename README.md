# Evil Dash: Geometry Dash Clone

A lightweight Geometry Dash clone built with Python and Pygame. Features smooth rotation physics, parallax backgrounds, and a high-score system.

## 🚀 How to Run (Linux/Arch)

First, install the system dependencies for Pygame:

```bash
sudo pacman -S python-pygame
